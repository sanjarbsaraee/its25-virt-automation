# Galaxy collections. Cloud-init installs them at first boot.
# Versions pinned so a new release does not break a fresh build.

collections:

  # Lets Ansible fetch db passwords and API keys from Infisical
  # at runtime instead of hardcoding them in playbooks.
  - name: infisical.vault
    version: "1.1.4"