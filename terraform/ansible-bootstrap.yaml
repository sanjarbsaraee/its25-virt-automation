#cloud-config
users:
  - name: admin
    groups: sudo
    shell: /bin/bash
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    ssh_authorized_keys:
      - ${sanjar_key}
      - ${jim_key}

package_update: true
package_upgrade: true
packages:
  - ansible
  - git
  - python3-pip